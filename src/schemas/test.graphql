type Users {
    id: ID!
    email: String
    email_confirmed: Boolean
    first_name: String
    last_name: String
    company_name: String
    company_description: String
}

type Query {
    Users(id: ID!): Users
}